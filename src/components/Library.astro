---
import { SkiCycleRunConfig } from "../skicyclerun.config";
// ImageMetadata is no longer exported from astro in newer versions
// import type { ImageMetadata } from "astro";
export function getRndValue(images: any) {
  const randomIndex = Math.floor(Math.random() * images.length);
  return images[randomIndex];
}

export async function getLocalImages() {
  const imageObject = import.meta.glob<{ default: ImageMetadata }>(
    "/src/assets/images/*.{jpeg,jpg,png,gif,svg}"
  );
  const imageKeys = Object.keys(imageObject);
  return imageKeys;
}

export async function getImgMeta(imgPath: string) {
  console.log("getImgMeta: ", imgPath);
  return imgPath;
}

export async function getAlbumPhotos(
  album: any,
  loraStyle: string = "random"
): Promise<any[]> {
  try {
    let photos: any[] = [];

    // Use local images only if explicitly requested or during SSG build
    if (album === "local") {
      const imagePaths = await getLocalImages();
      const imageModules = import.meta.glob<{ default: ImageMetadata }>(
        "/src/assets/images/*.{jpeg,jpg,png,gif,svg}"
      );

      photos = await Promise.all(
        imagePaths.map(async (path: string, i: number) => {
          const mod = imageModules[path];
          const img = mod ? await mod() : null;
          return {
            src: img?.default?.src ?? undefined, // ✅ Use the actual image path
            altText: `Photo ${i + 1}`,
            sourceType: "local",
          };
        })
      );
    } else {
      // Development mode - try API call with fallback to local
      try {
        const baseUrl =
          import.meta.env.PUBLIC_SKICYCLERUN_API ||
          "https://api.skicyclerun.com/v2/";
        const loraParam =
          loraStyle === "random" ? "" : `&loraStyle=${loraStyle}`;
        const photoURL = `${baseUrl}getphotosrandom?bucketName=skicyclerun.lib&albumPath=albums/${album}/&numPhotos=${SkiCycleRunConfig.photoApi.buildTimePhotos}${loraParam}`;
        const res = await fetch(photoURL);
        const data = await res.json();

        // ✅ Defensive check: only map if data is an array
        if (Array.isArray(data)) {
          photos = data.map((p: any, i: number) => {
            const src = typeof p === "string" ? p : p?.src;
            return {
              src,
              altText: p?.altText ?? `Photo ${i + 1}`,
              sourceType: "remote",
            };
          });
        } else {
          console.warn(
            "⚠️ Unexpected response format from photo API, falling back to local images"
          );
          photos = await getAlbumPhotos("local");
        }

        // ✅ Fallback for server errors
        if ((data as any)?.message === "Internal server error") {
          console.warn(
            "⚠️ Photo API server error, falling back to local images"
          );
          photos = await getAlbumPhotos("local");
        }
      } catch (apiError) {
        const loraParam =
          loraStyle === "random" ? "" : `&loraStyle=${loraStyle}`;
        console.warn("\n⚠️ Photo API failed, falling back to local images");
        console.warn(
          "📍 URL:",
          `${import.meta.env.PUBLIC_SKICYCLERUN_API || "https://api.skicyclerun.com/v2/"}getphotosrandom?bucketName=skicyclerun.lib&albumPath=albums/${album}/&numPhotos=${SkiCycleRunConfig.photoApi.buildTimePhotos}${loraParam}`
        );
        console.warn(
          "🔴 Error type:",
          apiError instanceof Error ? apiError.name : typeof apiError
        );
        console.warn(
          "🔴 Error message:",
          apiError instanceof Error ? apiError.message : String(apiError)
        );
        console.warn("📦 Album:", album);
        console.warn("🎨 LoRA Style:", loraStyle);
        console.warn("━━━━━━━━━━━━━━━━━━━━━━━━━━━\n");
        photos = await getAlbumPhotos("local");
      }
    }

    return photos;
  } catch (error) {
    console.log("ERROR:getAlbumPhotos", error);
    // Final fallback to empty array to prevent build failures
    return [];
  }
}

export function getRandomImage(
  imagesObject: Record<string, () => Promise<{ default: ImageMetadata }>>
) {
  const imagePaths = Object.keys(imagesObject);
  const randomIndex = Math.floor(Math.random() * imagePaths.length);
  const randomImagePath = imagePaths[randomIndex];

  return randomImagePath;
}

export function findKeyWithValue(
  searchString: string,
  obj: Record<string, any>
) {
  for (const key in obj) {
    console.log("findKeyWithValue: key == ", key);
    if (key.includes(searchString)) {
      return { key, value: obj[key] };
    }
  }
  return null;
}

// export function findSubstring(obj: Record<string, any>, target: string): any {
//   for (const key in obj) {
//     if (key.includes(target)) {
//       return obj[key];
//     }
//   }
//   return null;
// }

export function findSubstring(
  imageName: string,
  imageModules: Record<string, any>
) {
  for (const key in imageModules) {
    if (key.endsWith(imageName)) {
      return imageModules[key];
    }
  }
  console.warn("❌ No match found for:", imageName);
  return null;
}

export function getImage(imgSrc: unknown, imgObj: Record<string, any>) {
  if (typeof imgSrc !== "string") {
    console.warn("⚠️ getImage received non-string imgSrc:", imgSrc);
    return null;
  }

  console.log("🔍 Available image keys:", Object.keys(imgObj));
  console.log("🔍 Looking for match with:", imgSrc);

  const result = findSubstring(imgSrc, imgObj);
  if (!result) {
    console.warn("⚠️ getImage could not find match for:", imgSrc);
  }
  return result;
}

export function getFileName(path: unknown): string {
  if (typeof path === "object" && path !== null && "src" in path) {
    path = (path as any).src;
  }

  if (typeof path !== "string") return "imageNotFound";

  const cleanPath = path.split("?")[0];

  // Bail if it's a hashed Astro path or remote URL
  if (cleanPath.startsWith("/_astro/") || cleanPath.startsWith("http")) {
    return "imageNotFound";
  }

  const matchResult = cleanPath.match(
    /\/([\w-]+)(?:\.[\w]+)*\.(svg|jpeg|jpg|png|gif)$/i
  );
  if (matchResult) {
    return `${matchResult[1]}.${matchResult[2]}`;
  }
  console.warn("⚠️ - getFileName received non-string path:", path);
  return "imageNotFound";
}
---
